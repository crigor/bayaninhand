%h1 Search Results
%hr
.search-results
  %b>= "#{pluralize(@organizations_total_entries, "organization")} found"
  %ul
    - @organizations.each do |organization|
      %li
        = image_tag organization.image.url(:small), :class => "search-results-img", :style => "width:50px;height:50px"
        %span.search-results-desc
          = link_to organization.name, organization_path(organization)
          %br
      .clear
